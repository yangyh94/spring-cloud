info:
  app:
    name: "@project.name@"
    description: "@project.description@"
    version: "@project.version@"

server:
  port: 8810

spring:
  application:
    name: config-server
  freemarker:
    prefer-file-system-access: false
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/zjzddts/zddts
          username: 779172494@qq.com
          password: Qibo666
  #开启消息总线
#  cloud:
#    bus:
#      enabled: true
#      trace:
#        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka
  instance:
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    prefer-ip-address: true