info:
  app:
    name: "user-service"
    description: "user Service"
    version: "0.0.1-SNAPSHOT"

eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
spring:
  application:
    name: user-service

  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: false
  datasource:
    hikari:
      connection-test-query: SELECT 1
      minimum-idle: 1
      maximum-pool-size: 5
      pool-name: dbcp1
    driver-class-namefl: com.mysql.jdbc.Driver
    url: jdbc:mysql://118.24.97.112:3306/test?serverTimezone=CTT&useSSL=false&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource



#mybatis
mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
  #实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: com.grand.user_service.entity
  #    typeEnumsPackage: com.baomidou.springboot.entity.enums
  global-config:
    # 数据库相关配置
    db-config:
      #主键类型  AUTO:"数据库ID自增", INPUT:"用户输入ID",ID_WORKER:"全局唯一ID (数字类型唯一ID)", UUID:"全局唯一ID UUID";
      id-type: INPUT
      #字段策略 IGNORED:"忽略判断",NOT_NULL:"非 NULL 判断"),NOT_EMPTY:"非空判断"
      field-strategy: not_empty
      #驼峰下划线转换
      column-underline: true
      #数据库大写下划线转换
      #capital-mode: true
      #逻辑删除配置
      logic-delete-value: 0
      logic-not-delete-value: 1
      db-type: mysql
      #刷新mapper 调试神器
      refresh: true
    # 原生配置
    configuration:
      map-underscore-to-camel-case: true
      cache-enabled: false

#服务器端口
server:
  port: 8820

#logging:
#  level:
#    com.okni.okkong.tacticsservice.dao: DEBUG
#    io.swagger.models.parameters.AbstractSerializableParameter: error

